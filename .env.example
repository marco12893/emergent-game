# Vercel KV (Redis) - Automatically set by Vercel when deployed
# For local development, you can use Redis locally or leave empty for fallback
KV_REST_API_URL="your_kv_rest_api_url"
KV_REST_API_TOKEN="your_kv_rest_api_token"
KV_REST_API_READ_ONLY_TOKEN="your_kv_read_only_token"

# Application URLs
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
NEXT_PUBLIC_GAME_SERVER_URL="http://localhost:3000"

# Optional: For CORS if needed!
CORS_ORIGINS="*"